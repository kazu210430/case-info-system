This package exists to satisfy the local `CaseInfoSystem.VstoBuildAssets` package dependency.

On this machine, actual VSTO reference assemblies are resolved from the installed Visual Studio VSTO paths.
